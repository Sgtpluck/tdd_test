 <% @tweets.each_with_index do |tweet, n| %>
   <%= n+1 %>. <%= link_to tweet.body, tweet_path(tweet.id) %><br>
  <%= tweet.author %><br>
  <% end %> 

  <%= link_to 'Make a new tweet!', new_tweet_path %>
